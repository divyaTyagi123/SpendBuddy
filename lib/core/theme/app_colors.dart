import 'package:flutter/material.dart';

class AppColors{
  static const primary = Color(0xFF003366);
  static const secondary = Color(0xFF00509E);
  static const accent = Color(0xFF007ACC);
  static const light = Color(0xFF66A3FF);
  static const background = Color(0xFFCCE0FF);

  static const income = Color(0xFF2ECC71);
  static const expense = Color(0xFFE74C3C);
}